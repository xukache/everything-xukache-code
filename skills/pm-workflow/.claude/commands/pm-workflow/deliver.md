# /pm-workflow:deliver

Package delivery artifacts.

Read `.claude/skills/pm-workflow/references/commands/deliver.md`, then use the `product-manager` subagent and run final review with `quality-reviewer`.

