# 需求拆解方法论

AI 编程工具一次做好一个小任务的成功率远高于一次做一个大任务。把大需求拆成 AI 能"一次做对"的小任务，是高效使用 AI 编程的核心技能。

## 拆解三原则

### 1. 每个任务是原子操作

❌ 太大："重构整个用户模块"

✅ 合适：
- 任务 1：把 UserService 的数据库查询从原始 SQL 改成 ORM
- 任务 2：给 UserService 的每个 public 方法加单元测试
- 任务 3：把 UserController 的参数校验从手写改成 Zod
- 任务 4：统一 User 相关接口的错误返回格式

### 2. 每个任务有明确的完成标准

❌ 模糊："优化一下性能"

✅ 明确："getUserList 接口响应时间从 3s 降到 500ms 以内。跑 `pnpm test` 验证功能不变。跑 `ab -n 1000 -c 10` 验证性能目标。"

### 3. 任务之间有清晰的依赖关系

```
任务 1：创建 Order 模型和数据库表（独立，先做）
任务 2：实现 OrderService CRUD（依赖任务 1）
任务 3：实现 OrderController 接口（依赖任务 2）
任务 4：写集成测试（依赖任务 3）
任务 5：加权限校验（依赖任务 3）
→ 任务 4 和 5 可以并行。
```

## 拆解模板

### 新功能开发

**第一轮：设计**
- 分析需求，提出澄清问题
- 给出 2-3 个技术方案
- 确认方案

**第二轮：数据层**
- 创建数据模型 / 数据库表
- 写数据访问层代码
- 验证：能正确读写数据库

**第三轮：业务层**
- 实现核心业务逻辑
- 写单元测试
- 验证：测试通过

**第四轮：接口层**
- 实现 API 接口
- 加参数校验和权限
- 写接口测试
- 验证：接口能正确调用

**第五轮：收尾**
- 加错误处理和日志
- 更新文档
- 完整回归测试

### Bug 修复

1. 复现和定位（不要改代码）
2. 分析根因（给出假设）
3. 确认根因（验证假设）
4. 修复 + 写防回归测试
5. 验证修复 + 回归测试

### 重构

1. 写测试覆盖现有行为（安全网）
2. 小步重构（每次只改一个点）
3. 每次改完跑测试（确保不破坏功能）
4. 重复步骤 2 和 3
5. 清理（删除过渡代码、更新文档）

## 输出格式

拆解后的任务清单应包含：

```
任务 N：[层级标签] 任务描述
  依赖：任务 X / 无
  完成标准：具体可验证的标准
  是否可并行：是 / 否
```

## 实际例子

**需求：给电商系统加购物车功能**

拆解后：

1. **[数据]** 创建 cart 和 cart_item 表，写 Migration
   - 依赖：无
   - 完成标准：migrate up/down 都能跑通

2. **[模型]** 创建 Cart 和 CartItem 的 ORM 模型
   - 依赖：任务 1
   - 完成标准：能 CRUD 购物车数据

3. **[服务]** 实现 CartService
   - addItem、removeItem、updateQuantity、getCart、clearCart
   - 依赖：任务 2
   - 完成标准：单元测试全部通过

4. **[接口]** 实现 REST API
   - POST /api/cart/items
   - DELETE /api/cart/items/:id
   - PATCH /api/cart/items/:id
   - GET /api/cart
   - DELETE /api/cart
   - 依赖：任务 3
   - 完成标准：接口测试通过，权限校验正确

5. **[集成]** 和订单模块对接
   - 购物车结算 → 创建订单
   - 下单后清空购物车
   - 依赖：任务 4
   - 完成标准：集成测试通过

**执行方式：** 每个任务独立让 AI 完成，完成后验证再进入下一个。
