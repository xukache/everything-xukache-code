# /pm-workflow:review

Run quality review for a stage.

Read `.claude/skills/pm-workflow/references/commands/review.md`, then use the `quality-reviewer` subagent. For `init`, the review must fail unless clarification is user-confirmed and concepts are aligned.

