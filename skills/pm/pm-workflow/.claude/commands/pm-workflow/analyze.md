# /pm-workflow:analyze

Run Stage 01 demand analysis.

Read `.claude/skills/pm-workflow/references/commands/analyze.md`, then use the `demand-analyst` subagent only after `docs/workflow-state.json` has `clarification.status=user_confirmed` and `clarification.concepts_aligned=true`.

