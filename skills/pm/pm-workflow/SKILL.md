---
name: pm-workflow
description: 面向算法/研发人员的产品经理工作流。用于澄清业务需求、输出产品原型与开发文档，并按版本管理 Markdown 与 HTML 交付物。
---
# PM Workflow

把业务方的口头需求、零散材料或模糊想法，转成可确认、可演示、可开发、可版本管理的产品交付物：结构化需求上下文、可交互 HTML 原型、PRD、Mermaid 流程图、开发交接文档、需求笔记和版本化索引页。

这个技能默认服务于算法人员、研发人员或技术负责人：你不需要先成为专业产品经理，但需要用产品经理的方式把问题、价值、边界、规则和验收标准问清楚。

## 工作原则

- 先定义问题，再定义方案：不要一上来实现页面或算法流程，先确认业务问题、目标用户、业务价值和成功指标。
- 对话式采集需求：主动追问缺失信息，把业务方的自然语言转成可开发的结构化上下文。
- 三维度定义细节：需求范围、规则限制、验收标准必须同时存在。
- 三段式推进：先业务需求沟通梳理，再产品原型和相关文档输出，最后做原型和文档版本管理。
- 关键节点暂停确认：在问题定义、方案范围、原型结构、正式文档和最终交付前暂停，等待用户确认或修改。
- 外部参考只做启发：需求调研阶段可以搜索成熟产品、开源框架或可靠案例来发现隐藏问题，但搜索结果默认是待验证假设，不得直接扩大 MVP 范围。
- Markdown 为源，HTML 为演示：正式 PRD、流程图、开发交接和交付说明必须有 Markdown 源文件；HTML 用于评审演示和阅读入口。
- 演示优先，工程克制：HTML 原型服务于需求确认和开发沟通，不实现复杂后端、真实算法、复杂数据流或生产级状态机，除非用户明确要求。
- 原型完成后再打磨：HTML 原型初稿完成后，用 `impeccable` 做可用性、视觉层级、响应式、可访问性和交互状态检查；只修正体验质量，不新增未确认需求。
- 职责边界收敛：完成线是确认版产品需求文档和原型归档，不默认生成技术架构、数据库设计、正式 API 契约或开发排期。
- 版本化组织：同一产品线按迭代版本管理，避免不同需求、不同轮次的文件混在一起。

## 快速启动

当用户给出一个新需求时，先用 2-5 个问题补齐最关键上下文。不要一次问完所有细节；优先让对话能继续向前。

如果用户只给出模糊想法、功能愿望或一句“想做一个平台/工具”，首轮只允许输出：

- 候选理解：你认为用户可能想解决什么问题。
- 待验证假设：哪些内容只是推测，不能写成事实。
- 3-5 个关键澄清问题：优先问会改变产品形态、业务边界和验收标准的问题。
- 下一步说明：回答后才能进入问题定义、范围表或交付目录。

首轮澄清时禁止初始化目录、写入 `requirements.md`、输出 MVP 范围表、生成 PRD 或制作原型，除非用户明确要求先创建工作区。

如果需要初始化交付目录，使用脚本：

```bash
python skills/pm/pm-workflow/scripts/scaffold_iteration.py --root output/pm-work --product "<产品线>" --iteration "<迭代名称>" --slug "<英文短名>"
```

脚本会创建产品线/迭代目录、Markdown 文档、HTML 演示页、需求笔记和 assets 目录。创建后再根据真实需求替换占位内容。

## 阶段流程

### 阶段 1：业务需求沟通梳理

读取用户提供的材料，提取：

- 业务场景：谁在什么场景下遇到什么问题。
- 目标用户：一线业务、运营、销售、审核员、管理者、客户或系统角色。
- 当前流程：现在线下、系统内或跨系统如何处理。
- 痛点和代价：慢、错、漏、不可追踪、人工判断不一致、转化低、成本高等。
- 目标结果：希望用户完成什么动作，业务指标如何改善。
- 已知约束：系统边界、数据来源、权限、合规、时效、成本、算法能力边界。

如果用户只提供模糊想法或功能愿望，先按 `references/discovery-intake.md` 做问题陈述、JTBD、证据分层和阶段门槛判断；信息不足时，再按 `references/discovery-playbook.md` 的问题库追问。优先问会影响产品形态和开发边界的问题。

当需求涉及平台、工具、系统、算法工作台、数据流程、内部管理端、审核台、标注台或其他成熟领域时，在初步问题定义之后、MVP 范围确认之前，按 `references/discovery-intake.md` 的 External Reference Lens 和 `references/discovery-playbook.md` 的外部参考扫描规则执行网络/GitHub 搜索。搜索目标是发现用户可能遗漏的角色、状态、异常、权限、数据质量、审计或人工兜底需求。

外部参考扫描必须遵守最小边界原则：

- 最多选 3-5 个高可信来源，优先官方文档、成熟开源仓库、主流产品文档或被广泛使用的框架。
- 搜索发现只能作为启发输入，默认记录为 `待验证假设`、`后置` 或 `风险待确认`。
- 不得因为某个开源框架有复杂能力，就把插件市场、复杂权限、自动化工作流、技术架构、部署方案或高级配置纳入第一期。
- 只有当用户确认某个发现直接解决当前核心痛点时，才允许进入 `必须有`。
- 若当前环境无法联网，记录未执行原因，不阻塞需求澄清。

把已确认或暂定确认的关键信息同步沉淀到 `notes/requirements.md`，包括问题定义、范围取舍、业务规则、算法参与点、验收标准和后续决策项。

**暂停确认 1：问题定义**

输出 5-8 行问题定义摘要，让用户确认：

```text
目标用户：
业务问题：
现有流程：
核心痛点：
期望结果：
成功指标：
关键约束：
本次不做：
```

### 阶段 2：产品原型和相关文档输出

只有在问题定义已被用户确认，或用户明确允许“基于当前假设继续”时，才能进入本阶段。否则继续停留在阶段 1，按 `references/discovery-intake.md` 补齐问题陈述、JTBD、证据分层和交付准备度。

基于已确认的问题定义，输出产品方案草案：

- 功能范围：本次必须有、可以后置、明确不做。
- 角色权限：不同角色能看、能改、能审批、能导出什么。
- 关键流程：主流程、异常流程、人工兜底流程。
- 规则限制：字段规则、状态流转、阈值、优先级、算法输入输出、人工干预点。
- 数据口径：输入数据、输出数据、字段含义、刷新频率、质量要求。
- 验收标准：业务验收、交互验收、数据验收、异常验收。

对算法类需求，必须额外确认：

- 模型/算法在流程中的位置：推荐、预测、排序、识别、归因、生成、决策辅助或自动决策。
- 输入特征是否稳定、可获得、可解释。
- 输出是否需要置信度、原因解释、人工复核、回滚或审计。
- 离线效果指标和线上业务指标分别是什么。

**暂停确认 2：范围与规则**

用表格列出 `必须有 / 后置 / 不做 / 风险待确认`，等待用户确认后再做原型。

### 阶段 3：HTML 原型

生成高保真、可交互、可本地打开的 HTML 原型。优先单文件 HTML；当页面较多时使用一个索引页路由到多个页面。

开始设计原型前，必须先完成设计主题 preflight：

1. 读取 `references/design-theme-selection.md`。
2. 从已确认需求中判定产品类型、用户角色、信息密度、工作流复杂度和品牌/参考来源。
3. 从 `assets/design-themes/` 中生成候选主题；中后台、控制台、CRM、运营平台、内部工具、权限/表格/表单密集系统默认推荐 Vben；用户指定品牌、参考站点、截图或风格名时，可查找 `assets/design-themes/open-design/<slug>/DESIGN.md`。
4. 输出“设计方向确认表单”，包含产品类型、界面用途、主用户、核心工作流、信息密度、品牌/参考来源、推荐主题候选、样例预览路径、推荐理由和需要用户确认的问题。
5. 暂停等待用户确认主题。用户未确认前，禁止开始 HTML 原型设计；用户明确说“按你推荐的继续”时，使用第一推荐主题。
6. 读取被确认的主题文件，并把 token、组件、布局和 Do/Don't 作为 HTML 原型的主约束。
7. 把设计方向确认表单、候选主题、用户确认结果、产品语境、主题、源文件、样例预览路径、选择原因和补充建议写入 `notes/requirements.md` 的“原型设计输入”。
8. 再开始 HTML 原型设计。

`ui-ux-pro-max` 只用于补充布局、图表、可访问性、响应式和组件细节，不得覆盖已选主题。

原型要求：

- 首屏就是可用工作界面，不做营销落地页。
- 交互必须能演示主流程：筛选、录入、提交、审批、状态变更、查看结果、展开 PRD 浮标等。
- 使用模拟数据，数据要贴近业务真实语境。
- 用流程图或泳道图表达关键业务流、算法流或审批流。
- 避免实现复杂真实业务逻辑、真实 API、复杂数据流转；用 mock、局部状态和演示事件表达即可。
- 每个关键页面右下角提供可展开 PRD 浮标，写清该页面对应的需求说明、字段、规则和验收标准。

详细标准见 `references/artifact-standards.md` 和 `references/html-prd-template.md`。

原型初稿完成后、交付用户确认前，必须执行一次 `impeccable` 原型打磨关卡。使用方式按当前环境中可用的 `impeccable` skill 或命令执行；若无法运行工具，也要按其检查维度做人工审阅并记录未执行原因。打磨范围只包括：

- 可用性和主流程是否顺畅。
- 视觉层级、信息架构、空间节奏和对齐。
- 表格、筛选器、表单、弹窗、抽屉、状态标签和空/错/加载状态是否清晰。
- 响应式、键盘焦点、对比度、可访问性和 UX 文案。
- 是否存在明显设计反模式、拥挤、重叠、按钮文字溢出或与已选主题冲突的样式。

`impeccable` 不得改变已确认 MVP 范围、用户角色、业务流程、字段规则、PRD 浮标内容或设计主题；发现超出范围的建议，只能记录为后续决策项。

**暂停确认 3：原型确认**

交付原型后，要求用户从这几个角度确认：

- 页面结构是否贴近业务人员的实际工作路径。
- 核心字段是否完整、命名是否符合业务习惯。
- 关键流程是否能讲清楚。
- PRD 浮标里的规则和验收是否足够研发实现。
- 哪些内容要删减、补充或拆分到下一迭代。

### 阶段 4：正式文档整理

在用户确认原型后，输出正式文档。Markdown 是版本管理源文件，HTML 是阅读和演示入口；详细格式见 `references/artifact-standards.md`。

正式文档至少包含：

- `prd.md`：背景目标、用户场景、需求范围、页面功能、字段、规则、指标、验收标准、本期不做。
- `flow.md`：Mermaid 源码和流程说明，流程图必须能从 Markdown 中直接维护。
- `dev-handoff.md`：面向开发的需求交接，不替代技术架构、数据库设计、正式 API 契约或排期承诺。
- `final-delivery.md`：评审通过后的确认版交付说明。
- `notes/requirements.md`：业务沟通和中间关键信息沉淀。

若已生成 `prd.html`、`flow.html` 或 `dev-handoff.html`，必须保持它们与对应 Markdown 内容一致。

### 阶段 5：产品原型和文档版本管理

最终目录建议：

```text
pm-work/
  product-slug/
    v20260509-mvp/
      README.md
      prd.md
      flow.md
      dev-handoff.md
      final-delivery.md
      index.html
      prototype.html
      prd.html
      flow.html
      dev-handoff.html
      final-delivery.html
      notes/
        requirements.md
      assets/
```

`README.md` 是版本管理入口，`index.html` 是演示入口。若拆多页面，所有 HTML 页面都要能返回索引页。

交付时给出：

- 文件路径。
- 本次覆盖的需求范围。
- 已确认范围和后续决策项。
- 开发可直接使用的下一步建议。

最终交付完成后停止在需求交付边界内。除非用户明确要求进入研发实现，不继续输出技术方案、架构设计、数据库设计、正式 API 契约或排期计划。

## 质量检查

交付前自检：

- 是否已确认问题定义，而不是只复述解决方案。
- 是否有 `本次不做`，避免范围失控。
- 是否每条关键规则都有对应验收标准。
- 是否算法能力被放在正确位置，且有人工兜底。
- 是否外部参考扫描有来源链接，且搜索发现没有未经确认地进入 `必须有`。
- 是否在原型生成前完成设计方向确认表单，并获得用户对主题候选的确认。
- 是否原型能本地打开并演示主流程。
- 是否已完成 `impeccable` 原型打磨记录，且未借打磨新增未经确认的业务范围。
- 是否 `prd.md`、`flow.md`、`dev-handoff.md`、`final-delivery.md` 与 HTML 展示一致，没有孤立文档和过期描述。
- 是否流程图使用 Mermaid 源码，并沉淀在 `flow.md`。
- 是否中间关键信息已落到 `notes/requirements.md`。
- 是否文件按产品线和迭代版本组织。
- 是否在最终交付后停止，没有默认扩展到技术架构、数据库设计、正式 API 契约或开发排期。

## 参考资料

- 需求追问与澄清：`references/discovery-playbook.md`
- 需求梳理入口与阶段门槛：`references/discovery-intake.md`
- 原型设计主题选择：`references/design-theme-selection.md`
- HTML 原型与 PRD 交付标准：`references/artifact-standards.md`
- 页面右下角 PRD 浮标模板：`references/html-prd-template.md`
